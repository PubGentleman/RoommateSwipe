PROMPT: Fix All AI Chat Screens — Route Through Supabase Edge Functions
Every AI chat screen in the app is returning "Something went wrong. Try again." because they are calling Claude directly from the app instead of through Supabase Edge Functions. Fix all of them:

Screen 1 — Match Profile Chat ("Ask about [Name]")
Create a Supabase Edge Function called match-chat:

Accepts { question, currentUserId, matchedUserId }
Fetches both users' full profile data from the database
Calls Claude using ANTHROPIC_API_KEY from Supabase secrets
Prompt: "You are a helpful roommate matching assistant for Rhome. Answer this question about compatibility between two users based on their profiles. Current user: [DATA]. Matched user: [DATA]. Compatibility score: [SCORE]%. Question: [QUESTION]. Answer in 2-3 specific sentences."
Falls back to a data-driven answer if Claude fails
Never returns "Something went wrong"


Screen 2 — Neighborhood Chat ("Ask about this neighborhood")
Update the existing neighborhood-chat Edge Function to make sure it:

Accepts { question, neighborhood, city, listingId, latitude, longitude }
Fetches Overpass API amenity data and NYC crime data if available
Passes all real data as context to Claude
Never returns vague answers like "check local forums"
Falls back to amenity data if Claude fails


Screen 3 — Why Match Explanation ("Why?" button)
Create a Supabase Edge Function called explain-match:

Accepts { userId, matchedProfileId }
Fetches both users' profile data
Calls Claude to generate a 2-3 sentence explanation of why they match
Based on: budget overlap, location, move-in timing, room count, lifestyle tags, occupation
Falls back to a data-driven explanation if Claude fails
Never returns an error to the app


Screen 4 — AI Match Assistant (Messages screen)
Create a Supabase Edge Function called ai-match-assistant:

Accepts { question, userId }
Fetches the current user's full profile and preferences
Fetches their top matches from the database
Calls Claude to answer general roommate/neighborhood/matching questions
Context: user's preferences, location, budget, lifestyle
Falls back to helpful general advice if Claude fails
Never returns an error


General rules for ALL Edge Functions:

Every Edge Function must have a try/catch around the Claude API call
If Claude throws any error, always return a fallback response — never return an error message to the app
Add CORS headers to every Edge Function so the app can call them
Use this Claude model: claude-3-5-haiku-20241022 for speed and cost efficiency
Every function should respond within 10 seconds — set a timeout
Update every AI chat screen in the app to call its corresponding Edge Function instead of calling Claude directly