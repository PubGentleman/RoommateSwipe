Add a filter bottom sheet to the roommate match screen that opens when the filter icon is tapped next to the city pill. The sheet should slide up from the bottom with a drag handle at the top and a dark overlay behind it.
The filter sheet should have these sections:
Budget Range — a dual-handle range slider from $500 to $5,000/mo. Show the selected range dynamically (e.g. '$800 — $2,400/mo') above the slider as it moves.
Move-in Date — three quick-select chips: 'ASAP', 'Within 30 days', 'Within 3 months', plus a 'Pick a date' option that opens a date picker calendar.
Room Type — tap-to-select chips for: Private Room, Shared Room, Entire Place, Studio. Allow multiple selections.
Lifestyle — tap-to-select chips for: Pet Friendly, Non-Smoker, Remote Worker, Students OK, Night Owl, Early Bird. Allow multiple selections.
Search Radius — a segmented control with options: 5mi, 10mi, 25mi, 50mi.
At the bottom of the sheet show two buttons side by side: a ghost/outline 'Reset' button that clears all filters and a solid coral 'Apply Filters' button. Show a count badge on the Apply button that updates live as filters are selected (e.g. 'Apply · 3 filters').
When filters are applied, reload the match feed with filtered results and show a small filter summary bar below the city pill listing active filters as dismissible chips (e.g. '$800-$2,400' ✕, 'Pet Friendly' ✕). Tapping a chip removes that filter instantly.
Save the user's last used filters to their profile so they persist between sessions. Use the app's existing dark theme with coral (#ff6b5b) as the accent color for active states, sliders, and the apply button."


Follow-up prompts to add after:

"Add a compatibility score filter — let users set a minimum match percentage (e.g. only show 70%+ matches)"
"Show the number of matching profiles that update in real time as filters change, like 'Showing 47 roommates'"
"If filters return zero results, show a friendly empty state with a 'Relax filters' button that resets to defaults