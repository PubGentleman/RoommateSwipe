Technical Stack
	•	Python for the backend
	•	HTML/JS for the frontend
	•	SQL databases, never JSON file storage
	•	Separate databases for dev, test, and prod
	•	Elasticsearch for search, using Elastic.co hosting
	•	Elastic.co will have dev and prod indexes
	•	Python tests


Coding Pattern Preferences
	•	Always prefer simple solutions.
	•	Avoid duplication of code whenever possible, which means checking for other areas of the codebase that might already have similar code and functionality.
	•	Write code that takes into account the different environments: dev, test, and prod.
	•	Be careful to only make changes that are requested or changes you are confident are well understood and related to the requested change.
	•	When fixing an issue or bug, do not introduce a new pattern or technology without first exhausting all options for the existing implementation. If you do this, make sure to remove the old implementation afterward so there is no duplicate logic.
	•	Keep the codebase very clean and organized.
	•	Avoid writing scripts in files if possible, especially if the script is likely to be run only once.
	•	Avoid having files over 200–300 lines of code. Refactor at that point.
	•	Mocking data is only needed for tests; never mock data for dev or prod.
	•	Never add stubbing or fake data patterns to code that affect the dev or prod environments.
	•	Never overwrite your .env file without first asking and confirming.

Role Seperation:
There is three Role.
Make an Airbnb app, airbnb for roommates
Find and refactor code where renter and host features are mixed.  
1. Search for occurrences of "renter”, "host" and “agent/landlord” in the codebase.  
2. Ensure role-based logic is properly separated.  
3. Update the code so that renters and hosts only see their respective features.  
4. Apply conditional rendering in the frontend and role-based authorization in the backend.  
5. Suggest any improvements in role management to keep the code clean.  

Ensure the Renter role in the navigation bar has only the following sections: Explore, Roommates, Groups, Messages, and Profile.  

1. Verify that renters can search for potential roommates in the "Roommates" section and interact with them.  
2. Ensure that when two or more renters agree to search for a place together, they can form a group.  
3. The "Groups" section should display all groups across the platform. Renters should be able to browse groups and join those looking to rent an apartment.  
4. Remove any host-related features from the renter role.  
5. Implement role-based access control to ensure only renters can see these sections in the navigation bar.  
6. Suggest improvements for better code organization and maintainability.

Test with NPX EXPO LATEST

```mermaid
graph TD
    %% Login/Registration Flow
    subgraph Authentication
        Login[Login Screen]
        Register[Registration Screen]
        RoleSelect[Role Selection]
        Login --> Register
        Register --> RoleSelect
    end

    %% Renter Role Navigation
    subgraph RenterFlow[Renter Dashboard]
        RExplore[Explore Tab]
        RRoommates[Roommates Tab]
        RGroups[Groups Tab]
        RMessages[Messages Tab]
        RProfile[Profile Tab]
        
        RExplore --> |View Listings| ListingDetail
        RRoommates --> |View Profile| RoommateProfile
        RGroups --> |View Group| GroupDetail
        RMessages --> |Chat| ChatWindow
    end

    %% Host Role Navigation
    subgraph HostFlow[Host Dashboard]
        HListings[My Listings]
        HApplications[Applications]
        HMessages[Messages]
        HProfile[Profile]
        
        HListings --> |Manage| ListingManagement
        HApplications --> |Review| ApplicationDetail
    end

    %% Agent/Landlord Role Navigation
    subgraph AgentFlow[Agent Dashboard]
        AProperties[Properties]
        AVerification[Verification]
        ADocuments[Documents]
        AMessages[Messages]
        AProfile[Profile]
    end

    %% Common Components
    subgraph SharedComponents
        ListingDetail[Listing Detail View]
        RoommateProfile[Roommate Profile]
        GroupDetail[Group Detail]
        ChatWindow[Chat Interface]
        ListingManagement[Listing Management]
        ApplicationDetail[Application Review]
    end

    RoleSelect --> |Renter| RenterFlow
    RoleSelect --> |Host| HostFlow
    RoleSelect --> |Agent| AgentFlow

```

# Roommate Finder App - Wireframe Documentation

## Screen Descriptions

### Authentication Screens
1. **Login Screen**
   - Email/Username input
   - Password input
   - "Forgot Password" link
   - "Register" button
   - Social login options

2. **Registration Screen**
   - Email input
   - Password creation
   - Basic profile information
   - Role selection
   - Terms acceptance

### Renter Screens
1. **Explore Tab**
   - Property listing cards
   - Search filters
   - Map view toggle
   - Sort options
   - Save/favorite functionality

2. **Roommates Tab**
   - Roommate profile cards
   - Matching percentage
   - Filter by preferences
   - Quick message button
   - Save profiles option

3. **Groups Tab**
   - Active groups list
   - Create group button
   - Group invitations
   - Group chat preview
   - Member management

4. **Messages Tab**
   - Chat list
   - Unread indicators
   - Last message preview
   - Online status
   - Search conversations

5. **Profile Tab**
   - Profile picture
   - Personal information
   - Preferences settings
   - Lifestyle details
   - Budget range

### Host Screens
1. **My Listings**
   - Property list
   - Add new listing
   - Edit existing listings
   - View applications
   - Analytics dashboard

2. **Applications**
   - Application list
   - Status tracking
   - Review interface
   - Communication tools
   - Document verification

### Agent/Landlord Screens
1. **Properties**
   - Property portfolio
   - Add/Edit properties
   - Verification status
   - Analytics dashboard
   - Tenant management

2. **Documents**
   - Legal document storage
   - Document templates
   - Verification tools
   - Sharing interface
   - Archive system

## Common Components

### Listing Detail View
- Photo gallery
- Property details
- Amenities list
- Location map
- Contact options
- Application button

### Chat Interface
- Message thread
- Attachment support
- Typing indicators
- Read receipts
- Quick responses

### Profile View
- Profile picture
- Bio section
- Preferences display
- Compatibility score
- Contact options

## Design Guidelines
1. Use consistent padding and margins
2. Implement a clean, modern color scheme
3. Ensure touch targets are at least 44x44 points
4. Use clear typography hierarchy
5. Include loading states and error handling
6. Implement smooth transitions
7. Ensure accessibility compliance
8. Support dark/light mode 


Use Ai to find the apartment that fits the client best