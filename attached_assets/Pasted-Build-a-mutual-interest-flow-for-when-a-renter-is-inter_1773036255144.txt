Build a mutual interest flow for when a renter is interested in a listing or roommate profile. Replace any existing 'Apply' button with an 'I'm Interested' button on listing and profile cards.
When a renter taps 'I'm Interested', show a bottom sheet called an Interest Card that auto-fills from their profile with: profile photo and name, compatibility score with the host, budget range, desired move-in date, lifestyle tags (e.g. Clean, Non-Smoker, Remote Worker), and a single optional text field (max 150 characters) for a personal note like 'Why I'd be a great fit'. Show a 'Send Interest' button at the bottom.
When the interest card is sent: save it to the database under the host's notifications, show the renter a confirmation screen that says 'Interest Sent! You'll be notified when they respond' with an animated checkmark, and lock the listing with a 'Pending Response' badge so the renter can't send duplicate requests.
On the host side, show incoming interest requests as a new notification type. Each request shows the renter's Interest Card in full. Give the host two action buttons: 'Accept' (green) and 'Pass' (gray). If the host taps Accept: unlock the chat between both users, send both a push notification that says 'It's a Match! You can now message each other', and move the conversation into the Messages tab. If the host taps Pass: notify the renter with 'They passed this time' and free the listing so the renter can explore others.
Add a 'My Interests' section to the renter's profile tab showing all sent interest cards with status badges: Pending (yellow), Accepted (green), Passed (gray). Use the app's existing dark theme with coral (#ff6b5b) as the accent color throughout."


Follow-up prompts to add after:

"Add a 24-hour expiry on pending interest cards — if the host doesn't respond, auto-expire and notify the renter so they can move on"
"Add a 'Super Interest' feature for premium users that bumps their card to the top of the host's queue with a gold badge"
"Limit free users to 5 interest cards per day and prompt an upgrade when they hit the limit"

Want me to mock up what the Interest Card bottom sheet looks like? Sonnet 4.6