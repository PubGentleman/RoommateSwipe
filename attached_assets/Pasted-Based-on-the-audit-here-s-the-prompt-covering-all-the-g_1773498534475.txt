Based on the audit, here's the prompt covering all the gaps found:

```
Fix all outstanding issues found in the Roomdr codebase audit. 
Do not remove mock data or isDev guards. All fixes are additive.

────────────────────────────────────────
FIX 1 — PACKAGE NAME
────────────────────────────────────────

In package.json, change:
  "name": "my-app"
to:
  "name": "roomdr"

────────────────────────────────────────
FIX 2 — AI MEMORY LAYER
────────────────────────────────────────

Create utils/aiMemory.ts with the following:

import AsyncStorage from '@react-native-async-storage/async-storage';
import { isDev } from './envUtils';

const MEMORY_KEY = 'roomdr_ai_memory';

export interface AIMemory {
  totalSwipes: number;
  rightSwipes: number;
  leftSwipes: number;
  skipReasons: string[];
  avgMatchScore: number;
  matchScoreTrend: 'improving' | 'declining' | 'stable';
  skippedDueToDistance: number;
  skippedDueToSchedule: number;
  skippedDueToBudget: number;
  autoTightenedFilters: string[];
  totalConversations: number;
  coldConversations: string[];
  avgResponseRatePercent: number;
  lastActiveByChat: Record<string, string>;
  profileCompletionPercent: number;
  missingFields: string[];
  matchesBlockedByMissingFields: number;
  moveInDate: string | null;
  daysUntilMoveIn: number | null;
  refinementQuestionsAnswered: number;
  lastRefinementTimestamp: string | null;
  lastUpdated: string;
}

const DEFAULT_MEMORY: AIMemory = {
  totalSwipes: 0,
  rightSwipes: 0,
  leftSwipes: 0,
  skipReasons: [],
  avgMatchScore: 0,
  matchScoreTrend: 'stable',
  skippedDueToDistance: 0,
  skippedDueToSchedule: 0,
  skippedDueToBudget: 0,
  autoTightenedFilters: [],
  totalConversations: 0,
  coldConversations: [],
  avgResponseRatePercent: 0,
  lastActiveByChat: {},
  profileCompletionPercent: 0,
  missingFields: [],
  matchesBlockedByMissingFields: 0,
  moveInDate: null,
  daysUntilMoveIn: null,
  refinementQuestionsAnswered: 0,
  lastRefinementTimestamp: null,
  lastUpdated: new Date().toISOString(),
};

export async function getAIMemory(): Promise<AIMemory> {
  const raw = await AsyncStorage.getItem(MEMORY_KEY);
  if (!raw) return { ...DEFAULT_MEMORY };
  try { return { ...DEFAULT_MEMORY, ...JSON.parse(raw) }; }
  catch { return { ...DEFAULT_MEMORY }; }
}

export async function updateAIMemory(patch: Partial<AIMemory>): Promise<void> {
  const current = await getAIMemory();
  const updated = { ...current, ...patch, lastUpdated: new Date().toISOString() };
  await AsyncStorage.setItem(MEMORY_KEY, JSON.stringify(updated));
}

export async function resetAIMemory(): Promise<void> {
  if (!isDev) return;
  await AsyncStorage.removeItem(MEMORY_KEY);
}

// Call this after every swipe in RoommatesScreen
export async function recordSwipe(wasRight: boolean, matchScore?: number): Promise<void> {
  const mem = await getAIMemory();
  const totalSwipes = mem.totalSwipes + 1;
  const rightSwipes = wasRight ? mem.rightSwipes + 1 : mem.rightSwipes;
  const leftSwipes = !wasRight ? mem.leftSwipes + 1 : mem.leftSwipes;
  let avgMatchScore = mem.avgMatchScore;
  if (matchScore !== undefined) {
    avgMatchScore = ((mem.avgMatchScore * mem.totalSwipes) + matchScore) / totalSwipes;
  }
  await updateAIMemory({ totalSwipes, rightSwipes, leftSwipes, avgMatchScore });
}

// Call this after message activity
export async function recordMessageActivity(chatId: string): Promise<void> {
  const mem = await getAIMemory();
  const lastActiveByChat = { ...mem.lastActiveByChat, [chatId]: new Date().toISOString() };
  // Mark conversations cold if no activity in 5 days
  const coldConversations = Object.entries(lastActiveByChat)
    .filter(([, ts]) => {
      const daysSince = (Date.now() - new Date(ts).getTime()) / (1000 * 60 * 60 * 24);
      return daysSince >= 5;
    })
    .map(([id]) => id);
  await updateAIMemory({ lastActiveByChat, coldConversations });
}

────────────────────────────────────────
FIX 3 — WIRE AI MEMORY TO SWIPES
────────────────────────────────────────

In RoommatesScreen.tsx, import recordSwipe from utils/aiMemory.ts.

Find the swipe handler functions (handleLike, handlePass, or wherever 
right/left swipe actions are processed). After each swipe action, call:
  await recordSwipe(true, compatibilityScore);  // for right swipe
  await recordSwipe(false);                     // for left swipe

Find the message send handler in ChatScreen.tsx. After a message is 
successfully sent, call:
  await recordMessageActivity(chatId);

────────────────────────────────────────
FIX 4 — WIRE AI MICRO QUESTIONS
────────────────────────────────────────

The file utils/aiMicroQuestions.ts exists with 12 questions but is not 
triggered anywhere. Wire it up in RoommatesScreen.tsx:

Import: import { MICRO_QUESTIONS } from '../../utils/aiMicroQuestions';
Import: import { getAIMemory, updateAIMemory } from '../../utils/aiMemory';

After every 25 right swipes (check totalSwipes % 25 === 0 in recordSwipe), 
and if at least 3 hours have passed since the last micro question 
(check AIMemory.lastRefinementTimestamp), show the AI sheet with 
screenContext='refinement' and pass one unanswered micro question.

To pick the next unanswered question:
- Get user.aiAssistantData?.questionsAsked (array of question IDs already asked)
- Filter MICRO_QUESTIONS to exclude already-asked IDs
- Pick the first one from the filtered list
- If all are answered, don't trigger

After the user answers, call:
  await updateAIMemory({ 
    refinementQuestionsAnswered: mem.refinementQuestionsAnswered + 1,
    lastRefinementTimestamp: new Date().toISOString()
  });

────────────────────────────────────────
FIX 5 — TAPPABLE AI INSIGHT CARDS
────────────────────────────────────────

In RoomdrAISheet.tsx, the InsightCard component has an onTap prop but it 
is not connected to navigation. Wire up each card's onTap using the 
onNavigate prop already passed into RoomdrAISheet:

Profile Completion card:
  onTap={() => { onDismiss(); onNavigate?.('ProfileQuestionnaire', 
    { focusStep: missingFields[0] }); }}

Match Rate card:
  onTap={() => { onDismiss(); onNavigate?.('Roommates', { showStats: true }); }}

Match Pool Impact card:
  onTap={() => { onDismiss(); onNavigate?.('Preferences'); }}

Response Rate card:
  onTap={() => { onDismiss(); onNavigate?.('Messages', { filter: 'unanswered' }); }}

Add a chevron icon to every tappable card header row:
  <Feather name="chevron-right" size={14} color="rgba(255,255,255,0.3)" />

Wrap the entire InsightCard in a Pressable with a brief opacity flash 
(0.7 for 100ms) on press before calling onTap.

If onNavigate is not provided or the screen doesn't exist, catch the 
error and show a brief "Coming soon" toast instead of crashing.

────────────────────────────────────────
FIX 6 — PROFILE REMINDER OVERLAY
────────────────────────────────────────

In RootNavigator.tsx, ProfileReminderOverlay is imported but not receiving 
its trigger data. Fix this:

Import ProfileReminderContext: 
  import { useProfileReminderContext } from '../contexts/ProfileReminderContext';

Inside the RootNavigator component body, call:
  const { shouldShowReminder, reminderData, dismissReminder } = useProfileReminderContext();

Pass these to ProfileReminderOverlay:
  <ProfileReminderOverlay 
    visible={shouldShowReminder}
    heading={reminderData?.heading}
    subtext={reminderData?.subtext}
    onDismiss={dismissReminder}
    onNavigate={(screen, params) => navigation.navigate(screen, params)}
  />

If ProfileReminderContext does not have shouldShowReminder/reminderData/
dismissReminder exported yet, add them. The context should check 
profileCompletionPercent from AIMemory — if it's below 70% and the user 
has been using the app for more than 10 minutes in the session, set 
shouldShowReminder to true with an appropriate heading and subtext.

────────────────────────────────────────
FIX 7 — CREATE GROUP HARDCODED VALUES
────────────────────────────────────────

In CreateGroupScreen.tsx, replace the hardcoded values:

Change:
  budget: 2000,
  preferredLocation: 'San Francisco Bay Area',

To:
  budget: user?.profileData?.budget || 2000,
  preferredLocation: user?.profileData?.city || user?.profileData?.neighborhood 
    || 'Your City',

────────────────────────────────────────
FIX 8 — ADDRESS REVEAL AFTER HOST ACCEPTANCE
────────────────────────────────────────

In HostInquiriesScreen.tsx, when a host accepts an inquiry (acceptInterestCard), 
after the acceptance is confirmed update the group's addressRevealed field:

After a successful acceptInterestCard() call, find the matching group for 
this inquiry (match by listingId and renterId) and update it:
  await updateGroup(groupId, { 
    addressRevealed: true, 
    inquiryStatus: 'accepted' 
  });

In GroupsScreen.tsx, when rendering a listing_inquiry group card, check 
group.addressRevealed:
- If false or undefined: show the listing address as "Address revealed after 
  host accepts" with a lock icon (Feather "lock")
- If true: show the actual listingAddress with an unlock icon 
  (Feather "unlock") in green (#2ecc71)

In ChatScreen.tsx for listing_inquiry group chats, add a banner at the top 
of the chat when addressRevealed becomes true:
  "The host has accepted your inquiry. Address: [listingAddress]"
  Style this as a green success banner with a Feather "check-circle" icon.

────────────────────────────────────────
FIX 9 — PHONE VERIFICATION
────────────────────────────────────────

In VerificationScreen.tsx, the phone number and code state exists but 
no actual Supabase call is made. Wire it up:

Send OTP:
  const { error } = await supabase.auth.signInWithOtp({ phone: phoneNumber });
  If error, show Alert. If success, setPhoneSent(true).

Verify OTP:
  const { error } = await supabase.auth.verifyOtp({ 
    phone: phoneNumber, token: phoneCode, type: 'sms' 
  });
  If success, call syncVerificationToSupabase with phone verified: true.

Guard the actual Supabase calls with isDev:
  if (isDev) {
    // Mock: skip real OTP, just mark verified after 1 second
    setTimeout(() => { setPhoneSent(true); }, 500);
    return;
  }
  // Real Supabase call here

────────────────────────────────────────
IMPORTANT RULES
────────────────────────────────────────
- Do not remove any existing mock data or isDev guards
- All AsyncStorage keys must be prefixed with 'roomdr_ai_'
- Wrap all new navigation calls in try/catch
- Test each fix individually before moving to the next
- Do not change any existing UI styling or layout
```

This covers all 9 gaps from the audit in one prompt. The most impactful fixes are the AI Memory Layer, the micro-question trigger wiring, and the address reveal flow.