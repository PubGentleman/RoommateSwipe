Here's the updated Replit prompt incorporating the free host tier:

---

**PROMPT: Update Host Pricing — Add Free Host Tier + Finalize All Tiers**

Update the existing host subscription system to add a Free host tier and finalize the full 4-tier pricing model. Preserve all existing mock data and isDev guards throughout.

---

**1. Update utils/hostPricing.ts**

Replace the `HOST_PLANS` object to include the free tier as the first entry:

```ts
export const HOST_PLANS = {
  free: {
    label: 'Host Free',
    price: 0,
    listingsIncluded: 1,
    overagePerListing: 0,
    freeBoosts: 0,
    freeBoostDuration: null,
    simultaneousBoosts: 0,
    features: {
      included: [
        '1 active listing',
        'Basic inquiry management',
        'Standard placement in search',
      ],
      locked: [
        'Renter group browsing',
        'AI assistant',
        'Listing boosts',
        'Compatibility scores',
        'Verified host badge',
      ],
    },
  },
  starter: {
    label: 'Host Starter',
    price: 19.99,
    listingsIncluded: 1,
    overagePerListing: 0,
    freeBoosts: 1,
    freeBoostDuration: '24h',
    simultaneousBoosts: 1,
    features: {
      included: [
        '1 active listing',
        'Renter group browsing',
        'AI assistant (host modes)',
        '1 free 24-hr boost per month',
        'Verified host badge',
        'Inquiry management',
        'Compatibility scores',
      ],
      locked: [],
    },
  },
  pro: {
    label: 'Host Pro',
    price: 49.99,
    listingsIncluded: 5,
    overagePerListing: 0,
    freeBoosts: 2,
    freeBoostDuration: '72h',
    simultaneousBoosts: 3,
    features: {
      included: [
        'Up to 5 active listings',
        'Priority placement in search',
        'Renter group browsing',
        'AI assistant (host modes)',
        '2 free 72-hr boosts per month',
        'Up to 3 simultaneous boosts',
        'Advanced analytics',
        'Response rate tracking',
      ],
      locked: [],
    },
  },
  business: {
    label: 'Host Business',
    price: 99,
    listingsIncluded: 15,
    overagePerListing: 5,
    freeBoosts: 2,
    freeBoostDuration: '7d',
    simultaneousBoosts: 10,
    features: {
      included: [
        'Up to 15 active listings',
        '+$5/mo per listing after 15',
        '2 free 7-day boosts per month',
        'Up to 10 simultaneous boosts',
        'Bulk boost across listings',
        'Full analytics suite',
        'Bulk messaging tools',
        'Agent verification badge (add-on)',
        'Priority support',
      ],
      locked: [],
    },
  },
};
```

Update `HostPlanType` in `types/models.ts`:
```ts
export type HostPlanType = 'free' | 'none' | 'starter' | 'pro' | 'business';
```

Update `canAddListing()` — free plan follows the same hard cap rule as starter (1 listing max, prompt to upgrade to Starter not Pro when blocked).

Update `calculateHostMonthlyCost()` — free plan always returns 0.

---

**2. Update screens/host/HostSubscriptionScreen.tsx**

Add the free tier card as the first plan in the grid. Layout for free card:

- Gray badge labeled "Free"
- Price: $0/mo
- "No credit card required" subtext
- Show included features with standard checkmarks
- Show locked features (renter group browsing, AI, boosts, compatibility scores, verified badge) grayed out with ✕ marks
- CTA button: "Current Plan" (disabled, outlined) if already on free, otherwise "Downgrade" 
- Add upgrade nudge at bottom of free card: "Hosts on Starter fill rooms faster with renter group access" — styled as a subtle purple info box
- If user is already on free plan: highlight their card with a border, show "Your current plan" label

Free plan is the **default** for all new host accounts. When a host first registers, set `plan: 'free'` automatically — no payment required, no onboarding friction.

---

**3. Update screens/host/HostDashboardScreen.tsx**

When a host is on the free plan:
- Show a dismissible upgrade banner at the top of the dashboard: "You're on the Free plan. Upgrade to browse renter groups and fill your room faster."
- Banner has two buttons: "Remind me later" (dismisses for 24hrs via AsyncStorage) and "See Plans" (navigates to HostSubscriptionScreen)
- Do NOT show this banner more than once per 24 hours

---

**4. Update screens/host/BrowseRenterGroupsScreen.tsx**

If host is on free plan:
- Show the screen but blur/lock the group cards
- Show an overlay prompt: "Upgrade to Starter to browse renter groups" with a CTA button
- Do not make any network calls to load groups if plan is free (save bandwidth)

---

**5. Update RoomdrAISheet.tsx**

If host context (`host_dashboard`, `host_listings`, `host_inquiries`) is triggered and user is on free plan:
- Show a locked state instead of insights: "AI insights are available on Starter and above"
- Show a small "Upgrade" button that navigates to HostSubscriptionScreen
- Do not load or compute insights for free plan hosts

---

**6. Update StorageService.ts**

Change default host subscription for new users:
```ts
static defaultHostSubscription(): HostSubscription {
  return {
    plan: 'free',
    listingsIncluded: 1,
    activeListingCount: 0,
    overagePerListing: 0,
    monthlyPrice: 0,
    freeBoostsRemaining: 0,
    freeBoostDuration: null,
    isVerifiedAgent: false,
    agentVerificationPaid: false,
  };
}
```

In isDev: seed one host mock user at `plan: 'free'`, one at `plan: 'starter'`, one at `plan: 'pro'`, one at `plan: 'business'` so all four states can be tested.

---

**7. Update Supabase migration**

```sql
-- Change default plan from 'none' to 'free'
ALTER TABLE users ALTER COLUMN host_plan SET DEFAULT 'free';

-- Backfill any existing 'none' values to 'free'
UPDATE users SET host_plan = 'free' WHERE host_plan = 'none' OR host_plan IS NULL;
```

---

**8. Update HostPlanBadge.tsx**

Add free tier badge styling:
```tsx
case 'free':
  return <Badge label="Free" backgroundColor="#1A1A1A" textColor="#888888" />;
```

---

**Guard rails:**
- Free plan hosts can post 1 listing and receive inquiries — never block this core action
- Never show boost UI to free plan hosts — hide the boost button entirely on their listing cards
- canAddListing() for free plan: if they try to add a second listing, prompt upgrade to Starter (not Pro or Business)
- All Supabase calls must have try/catch with StorageService fallback
- Preserve all existing mock data — do not wipe isDev seeds

---

Want me to also update the business model slide in the pitch deck to show the 4-tier host model including the free tier?