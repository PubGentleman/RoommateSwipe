---

**REPLIT PROMPT — Replace Occupation Text Input with Tag Selector on Location & Work Screen**

The "Location & Work" screen (Step 4 of 14) currently has a free text input field for Occupation. Replace that text input with the existing `<TagSelector>` component using the `OCCUPATION_TAGS` data. Do not move occupation to a different screen — keep it exactly where it is on this screen.

---

**WHAT TO CHANGE**

In the Location & Work step screen, find the Occupation section which currently renders a plain text input field showing placeholder text like "Software Engineer".

Remove:
- The free text `<TextInput>` for occupation
- Any `onChangeText` handler tied to a free text occupation string
- Any keyboard handling for that field

Replace with:
- The label **"Occupation"** kept exactly as it is
- A small subtext line below the label: **"Select the option that best describes what you do"** in gray, 13px
- The `<TagSelector>` component imported from `components/TagSelector.tsx` using:
  - `tags={OCCUPATION_TAGS}` from `constants/interestTags.ts`
  - `singleSelect={true}`
  - `showCount={false}`
  - `selectedTags={occupation ? [occupation] : []}`
  - `onChange={(tags) => setOccupation(tags[0] ?? null)}`

The tag selector should render inline on the screen — not in a modal or bottom sheet. It scrolls naturally with the rest of the screen content since the screen should already be inside a `<ScrollView>`.

---

**NEXT BUTTON BEHAVIOR**

The existing **"Next →"** coral button at the bottom should remain exactly as it is. Do not make the Next button require an occupation selection — occupation remains optional on this screen. If no tag is selected, `occupation` saves as `null` and the user can still proceed.

---

**SAVE THE VALUE**

Wherever the screen currently saves the occupation free text value to state or navigation params, update it to save the selected tag id string instead. For example if it was:

```ts
setOccupation(text)  // old free text
```

It becomes:

```ts
setOccupation(tags[0] ?? null)  // new tag id like 'software_engineer'
```

Make sure the saved value flows into the profile data the same way the old text value did.

---

**After completing, verify:**
1. The free text occupation input is gone from the Location & Work screen
2. The tag selector renders inline on the screen with all occupation categories and pills
3. Tapping a tag selects it with the coral highlight — tapping again deselects it
4. Only one occupation tag can be selected at a time
5. The Next button still works whether or not an occupation is selected
6. The selected occupation id saves correctly to profile state
7. App compiles with no TypeScript errors

---