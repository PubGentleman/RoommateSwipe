Prompt 1 — Fix Inconsistent Upgrade Navigation
In screens/renter/GroupsScreen.tsx, find every place that says:
javascriptnavigation.navigate('Payment')
And change it to:
javascriptnavigation.navigate('Plans')
Do the same in any other screen in the groups flow that navigates to 'Payment' when a subscription upgrade is needed. All upgrade prompts should go to 'Plans' not 'Payment'.

Prompt 2 — Fix CreateGroupScreen Navigation After Success
In screens/shared/CreateGroupScreen.tsx, after a group is successfully created in Supabase, the code tries to navigate to Messages > Chat. Replace that entire post-creation navigation block with this:
javascriptnavigation.goBack();
That's it. Just go back to the groups screen after creating. The user can open the group from My Groups tab. Do not try to navigate cross-stack into Messages.

Prompt 3 — Remove Duplicate Create Tab from GroupsScreen
In screens/renter/GroupsScreen.tsx, the "create" tab renders an inline group creation form. This duplicates CreateGroupScreen.tsx. Replace the entire create tab content with this:
javascriptif (activeTab === 'create') {
  navigation.navigate('CreateGroup');
  setActiveTab('discover');
  return null;
}
So tapping the Create tab immediately opens CreateGroupScreen instead of showing the inline form. Remove all the inline groupName, groupDescription, selectedListingId, selectedListingBedrooms state and handleCreateGroup function from GroupsScreen since they are no longer needed.

Prompt 4 — Admin "Who Liked My Group" Visibility
In screens/shared/GroupInfoScreen.tsx, find the section that shows group members. After the members list, add a "Likes" section that is only visible to the group admin (isAdmin === true).
Fetch users who have liked this group from the group_likes table where group_id = groupId. Show each user as a small row with their avatar, name, and two buttons: Invite (calls addMemberToGroup or sends an invite) and Ignore (dismisses).
If there are no likes yet, show: "No one has liked your group yet" in muted text.
Gate this section with isAdmin — regular members never see it.

Prompt 5 — Require Basic Profile Before Discovering Groups
In screens/renter/GroupsScreen.tsx, at the top of the discover tab render, before showing the swipe cards, check if the user has set a budget. Use:
javascriptconst hasPreferences = user?.profileData?.budgetMin || user?.profileData?.budget;
If hasPreferences is falsy, instead of showing the swipe cards show an empty state:
javascript<View style={styles.emptyState}>
  <Feather name="sliders" size={48} color={theme.textSecondary} />
  <ThemedText style={{ color: theme.textSecondary, textAlign: 'center', marginTop: 16 }}>
    Set your apartment preferences first so we can find the right groups for you.
  </ThemedText>
  <Pressable
    style={{ marginTop: 20, backgroundColor: theme.primary, paddingHorizontal: 24, paddingVertical: 12, borderRadius: 24 }}
    onPress={() => navigation.navigate('ApartmentPreferences')}
  >
    <ThemedText style={{ color: '#fff', fontWeight: '700' }}>Set Preferences</ThemedText>
  </Pressable>
</View>
Once the user sets preferences and returns, the swipe cards appear normally.