# Rhome — Instagram Verification + Multi-Photo Enforcement
## Replit Agent Prompt

---

## Overview

This prompt adds three connected features to Rhome:

1. **Instagram OAuth verification** — users connect their Instagram to prove they're real. We store a verified badge, NOT a scraped feed.
2. **Badge vs. Handle monetization gate** — the verification badge is visible to everyone on the swipe card, but the actual `@handle` is only revealed after a mutual match. Free users see a blurred handle with an upgrade prompt at peak intent.
3. **Mandatory multi-photo enforcement** — users must upload at least 3 photos before going live. A second optional category ("Photos of your space") is encouraged for hosts.

---

## Part 1 — Database Migration

Create file: `supabase/migrations/026_instagram_photos.sql`

```sql
-- Instagram verification fields on profiles
ALTER TABLE profiles
  ADD COLUMN IF NOT EXISTS instagram_handle TEXT,
  ADD COLUMN IF NOT EXISTS instagram_verified BOOLEAN DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS instagram_connected_at TIMESTAMPTZ;

-- Enforce minimum photo count at the DB level
-- photos is already a TEXT[] column — add a check constraint
ALTER TABLE profiles
  ADD CONSTRAINT min_photos CHECK (
    array_length(photos, 1) IS NULL OR array_length(photos, 1) >= 1
  );

-- Space photos — separate array for room/apartment photos
ALTER TABLE profiles
  ADD COLUMN IF NOT EXISTS space_photos TEXT[] DEFAULT '{}';

-- Profile completeness view used by the swipe deck query
-- A profile is "live" only if it has >= 3 total photos
CREATE OR REPLACE VIEW live_profiles AS
SELECT *
FROM profiles
WHERE
  (array_length(photos, 1) >= 3)
  AND full_name IS NOT NULL
  AND budget_min IS NOT NULL;
```

Run this migration in Supabase SQL Editor.

---

## Part 2 — Supabase Edge Function: `instagram-oauth`

Create file: `supabase/functions/instagram-oauth/index.ts`

```typescript
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

serve(async (req) => {
  if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders })

  try {
    const supabase = createClient(
      Deno.env.get('SUPABASE_URL')!,
      Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
    )

    const authHeader = req.headers.get('Authorization')!
    const token = authHeader.replace('Bearer ', '')
    const { data: { user } } = await supabase.auth.getUser(token)
    if (!user) return new Response(JSON.stringify({ error: 'Unauthorized' }), { status: 401, headers: corsHeaders })

    const { code } = await req.json()

    // Exchange code for access token with Meta
    const tokenRes = await fetch('https://api.instagram.com/oauth/access_token', {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: new URLSearchParams({
        client_id: Deno.env.get('INSTAGRAM_CLIENT_ID')!,
        client_secret: Deno.env.get('INSTAGRAM_CLIENT_SECRET')!,
        grant_type: 'authorization_code',
        redirect_uri: Deno.env.get('INSTAGRAM_REDIRECT_URI')!,
        code,
      }),
    })

    const tokenData = await tokenRes.json()
    if (!tokenData.access_token) {
      return new Response(JSON.stringify({ error: 'Instagram auth failed' }), { status: 400, headers: corsHeaders })
    }

    // Fetch the user's Instagram username
    const profileRes = await fetch(
      `https://graph.instagram.com/me?fields=id,username&access_token=${tokenData.access_token}`
    )
    const profileData = await profileRes.json()

    // Save to profiles — verified handle, never the access token
    const { error } = await supabase
      .from('profiles')
      .update({
        instagram_handle: profileData.username,
        instagram_verified: true,
        instagram_connected_at: new Date().toISOString(),
      })
      .eq('id', user.id)

    if (error) throw error

    return new Response(
      JSON.stringify({ success: true, handle: profileData.username }),
      { headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
    )
  } catch (err) {
    return new Response(JSON.stringify({ error: err.message }), { status: 500, headers: corsHeaders })
  }
})
```

Deploy: `supabase functions deploy instagram-oauth`

Add secrets in Supabase Edge Functions → Secrets:
- `INSTAGRAM_CLIENT_ID` — from Meta Developer App
- `INSTAGRAM_CLIENT_SECRET` — from Meta Developer App
- `INSTAGRAM_REDIRECT_URI` — your app's deep link (e.g. `rhome://instagram-callback`)

> **Note for developer:** Create a Meta Developer App at developers.facebook.com, enable Instagram Basic Display, and add the redirect URI. For Expo, use `expo-auth-session` with `makeRedirectUri`.

---

## Part 3 — Instagram Service

Create file: `services/instagramService.ts`

```typescript
import * as AuthSession from 'expo-auth-session'
import { supabase } from './supabase'

const INSTAGRAM_CLIENT_ID = process.env.EXPO_PUBLIC_INSTAGRAM_CLIENT_ID!

export const connectInstagram = async (): Promise<{ success: boolean; handle?: string; error?: string }> => {
  try {
    const redirectUri = AuthSession.makeRedirectUri({ scheme: 'rhome', path: 'instagram-callback' })

    const authUrl =
      `https://api.instagram.com/oauth/authorize` +
      `?client_id=${INSTAGRAM_CLIENT_ID}` +
      `&redirect_uri=${encodeURIComponent(redirectUri)}` +
      `&scope=user_profile` +
      `&response_type=code`

    const result = await AuthSession.startAsync({ authUrl })

    if (result.type !== 'success' || !result.params.code) {
      return { success: false, error: 'Instagram authorization cancelled' }
    }

    const { data: { session } } = await supabase.auth.getSession()
    const response = await fetch(
      `${process.env.EXPO_PUBLIC_SUPABASE_URL}/functions/v1/instagram-oauth`,
      {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          Authorization: `Bearer ${session?.access_token}`,
        },
        body: JSON.stringify({ code: result.params.code }),
      }
    )

    const data = await response.json()
    if (data.error) return { success: false, error: data.error }

    return { success: true, handle: data.handle }
  } catch (err: any) {
    return { success: false, error: err.message }
  }
}

export const disconnectInstagram = async (): Promise<void> => {
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return

  await supabase
    .from('profiles')
    .update({
      instagram_handle: null,
      instagram_verified: false,
      instagram_connected_at: null,
    })
    .eq('id', user.id)
}
```

---

## Part 4 — Instagram Verification Badge Component

Create file: `components/InstagramBadge.tsx`

```tsx
import React from 'react'
import { View, Text, TouchableOpacity, Linking, StyleSheet } from 'react-native'
import { Ionicons } from '@expo/vector-icons'

interface InstagramBadgeProps {
  verified: boolean
  handle?: string          // only passed if the viewer has permission to see it
  isBlurred?: boolean      // true = free user viewing a matched profile
  onUpgradePress?: () => void
}

export const InstagramBadge: React.FC<InstagramBadgeProps> = ({
  verified,
  handle,
  isBlurred = false,
  onUpgradePress,
}) => {
  if (!verified) return null

  const openInstagram = () => {
    if (handle) Linking.openURL(`https://instagram.com/${handle}`)
  }

  // Case 1: Viewer has access — show tappable handle
  if (handle && !isBlurred) {
    return (
      <TouchableOpacity style={styles.badge} onPress={openInstagram} activeOpacity={0.7}>
        <Ionicons name="logo-instagram" size={14} color="#E1306C" />
        <Text style={styles.handle}>@{handle}</Text>
        <Ionicons name="open-outline" size={12} color="#999" />
      </TouchableOpacity>
    )
  }

  // Case 2: Mutual match but free plan — blurred handle with upgrade CTA
  if (isBlurred && onUpgradePress) {
    return (
      <TouchableOpacity style={styles.badge} onPress={onUpgradePress} activeOpacity={0.8}>
        <Ionicons name="logo-instagram" size={14} color="#E1306C" />
        <Text style={[styles.handle, styles.blurred]}>@••••••••</Text>
        <View style={styles.lockBadge}>
          <Ionicons name="lock-closed" size={10} color="#fff" />
          <Text style={styles.lockText}>Upgrade</Text>
        </View>
      </TouchableOpacity>
    )
  }

  // Case 3: Verified badge only — no handle visible yet (not yet matched)
  return (
    <View style={styles.badge}>
      <Ionicons name="logo-instagram" size={14} color="#E1306C" />
      <Text style={styles.verifiedText}>Instagram Verified</Text>
      <Ionicons name="checkmark-circle" size={13} color="#4CAF50" />
    </View>
  )
}

const styles = StyleSheet.create({
  badge: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#1a1a2e',
    borderRadius: 20,
    paddingHorizontal: 10,
    paddingVertical: 5,
    gap: 5,
    alignSelf: 'flex-start',
  },
  handle: {
    color: '#fff',
    fontSize: 13,
    fontWeight: '500',
  },
  blurred: {
    color: '#aaa',
    letterSpacing: 2,
  },
  verifiedText: {
    color: '#ccc',
    fontSize: 12,
  },
  lockBadge: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#FF6B6B',
    borderRadius: 10,
    paddingHorizontal: 6,
    paddingVertical: 2,
    gap: 3,
  },
  lockText: {
    color: '#fff',
    fontSize: 10,
    fontWeight: '700',
  },
})
```

---

## Part 5 — Renter Swipe Card Updates

In `components/RenterCard.tsx` (or wherever the swipe card is rendered):

Find the section that renders the renter's name/bio and add the Instagram badge below it.

The card receives a `profile` object and a `viewerSubscriptionStatus` prop.

```tsx
// Add to RenterCard props
interface RenterCardProps {
  profile: Profile
  viewerHasPremium: boolean
  isMatched: boolean  // true if both users have swiped right
  onUpgradePress: () => void
}

// Inside the card render, below the name/age line:
<InstagramBadge
  verified={profile.instagram_verified}
  handle={
    // Show handle only if matched AND viewer has premium
    profile.isMatched && viewerHasPremium
      ? profile.instagram_handle
      : undefined
  }
  isBlurred={
    // Show blur/upgrade prompt if matched but NOT premium
    profile.isMatched && !viewerHasPremium && profile.instagram_verified
  }
  onUpgradePress={onUpgradePress}
/>
```

The swipe deck query already uses the `live_profiles` view, so unverified or incomplete profiles won't appear.

---

## Part 6 — Match Screen Handle Reveal

In `screens/MatchScreen.tsx` or wherever the match modal appears after both users swipe right:

```tsx
// This is the highest-intent moment in the app — show the handle or the upgrade gate here too
<View style={styles.matchInstagramSection}>
  <Text style={styles.sectionLabel}>Connect outside Rhome</Text>
  <InstagramBadge
    verified={matchedProfile.instagram_verified}
    handle={
      viewerHasPremium ? matchedProfile.instagram_handle : undefined
    }
    isBlurred={!viewerHasPremium && matchedProfile.instagram_verified}
    onUpgradePress={() => navigation.navigate('Upgrade')}
  />
</View>
```

If neither user has Instagram connected, show nothing in this section.

---

## Part 7 — Profile Settings Screen (Connect Instagram)

In `screens/ProfileScreen.tsx` or `screens/EditProfileScreen.tsx`, add an Instagram connection section:

```tsx
import { connectInstagram, disconnectInstagram } from '../services/instagramService'

// Inside the component:
const [instagramLoading, setInstagramLoading] = useState(false)

const handleConnectInstagram = async () => {
  setInstagramLoading(true)
  const result = await connectInstagram()
  if (result.success) {
    Alert.alert('Connected!', `@${result.handle} is now linked to your profile.`)
    // Refresh profile data
    await refreshProfile()
  } else {
    Alert.alert('Error', result.error || 'Could not connect Instagram')
  }
  setInstagramLoading(false)
}

// Render:
<View style={styles.instagramSection}>
  <View style={styles.instagramHeader}>
    <Ionicons name="logo-instagram" size={22} color="#E1306C" />
    <Text style={styles.instagramTitle}>Instagram Verification</Text>
  </View>
  <Text style={styles.instagramSubtitle}>
    Verified profiles get more matches. Your handle is only shown after a mutual match.
  </Text>

  {profile.instagram_verified ? (
    <View>
      <View style={styles.connectedRow}>
        <Ionicons name="checkmark-circle" size={18} color="#4CAF50" />
        <Text style={styles.connectedText}>@{profile.instagram_handle} connected</Text>
      </View>
      <TouchableOpacity
        style={styles.disconnectButton}
        onPress={disconnectInstagram}
      >
        <Text style={styles.disconnectText}>Disconnect</Text>
      </TouchableOpacity>
    </View>
  ) : (
    <TouchableOpacity
      style={styles.connectButton}
      onPress={handleConnectInstagram}
      disabled={instagramLoading}
    >
      {instagramLoading ? (
        <ActivityIndicator color="#fff" />
      ) : (
        <>
          <Ionicons name="logo-instagram" size={18} color="#fff" />
          <Text style={styles.connectButtonText}>Connect Instagram</Text>
        </>
      )}
    </TouchableOpacity>
  )}
</View>
```

---

## Part 8 — Mandatory Photo Enforcement

### 8a — Photo Upload Screen Updates

In `screens/EditProfileScreen.tsx` or `screens/PhotoUploadScreen.tsx`:

```tsx
const MIN_PHOTOS = 3
const MAX_PHOTOS = 9

// Photo validation before allowing profile to go live
const canGoLive = (photos: string[]): boolean => {
  return photos.length >= MIN_PHOTOS
}

// Photo counter UI — show above the upload grid
<View style={styles.photoCountRow}>
  <Text style={styles.photoCountText}>
    {photos.length}/{MIN_PHOTOS} required photos
  </Text>
  {photos.length >= MIN_PHOTOS ? (
    <View style={styles.photoCountBadge}>
      <Ionicons name="checkmark-circle" size={16} color="#4CAF50" />
      <Text style={styles.photoCountComplete}>Good to go!</Text>
    </View>
  ) : (
    <Text style={styles.photoCountRemaining}>
      Add {MIN_PHOTOS - photos.length} more to go live
    </Text>
  )}
</View>

// Space photos section (optional but encouraged for hosts)
<View style={styles.spacePhotosSection}>
  <Text style={styles.spacePhotosTitle}>Photos of your space</Text>
  <Text style={styles.spacePhotosSubtitle}>
    Optional — but profiles with room photos get 3× more interest from renters.
  </Text>
  {/* Render spacePhotos upload grid here — same pattern as main photos */}
</View>
```

### 8b — Prevent Going Live Without Minimum Photos

In the profile save / go-live flow, add this guard:

```tsx
const handleSaveProfile = async () => {
  if (photos.length < MIN_PHOTOS) {
    Alert.alert(
      'More photos needed',
      `Add at least ${MIN_PHOTOS} photos to make your profile visible to matches. You have ${photos.length} so far.`,
      [{ text: 'Add Photos', onPress: () => scrollToPhotoSection() }]
    )
    return
  }
  // Proceed with save
  await saveProfile()
}
```

### 8c — Onboarding Photo Step

If there is a multi-step onboarding flow, update the photo step to block progression:

```tsx
// In onboarding step navigation
const canProceedFromPhotoStep = photos.length >= MIN_PHOTOS

<TouchableOpacity
  style={[styles.nextButton, !canProceedFromPhotoStep && styles.nextButtonDisabled]}
  onPress={canProceedFromPhotoStep ? goToNextStep : showPhotoWarning}
  disabled={!canProceedFromPhotoStep}
>
  <Text style={styles.nextButtonText}>
    {canProceedFromPhotoStep ? 'Continue' : `Need ${MIN_PHOTOS - photos.length} more photo${MIN_PHOTOS - photos.length !== 1 ? 's' : ''}`}
  </Text>
</TouchableOpacity>
```

### 8d — Supabase Profile Save Guard

In `services/profileService.ts`, add photo validation before any upsert:

```typescript
export const saveProfile = async (profileData: Partial<Profile>): Promise<void> => {
  if (profileData.photos && profileData.photos.length < 3) {
    throw new Error('At least 3 photos are required to save your profile.')
  }

  const { error } = await supabase
    .from('profiles')
    .upsert(profileData)

  if (error) throw error
}
```

---

## Part 9 — Swipe Deck Query Update

In the service or hook that fetches profiles for the swipe deck, update the query to use the `live_profiles` view (created in migration 026) and exclude profiles with fewer than 3 photos:

```typescript
// In roommateService.ts or wherever swipe profiles are fetched
const { data, error } = await supabase
  .from('live_profiles')  // uses the view — already filters for >= 3 photos
  .select('*')
  .neq('id', currentUserId)
  // ... existing filters (budget, location, etc.)
```

If you prefer to keep the raw `profiles` table query, add this filter:

```typescript
.filter('photos', 'cs', '{"","",""}')  // less reliable — prefer the view
// Better:
.gte('array_length(photos, 1)', 3)     // or handle in .eq conditions
```

The `live_profiles` view is the cleanest approach.

---

## Part 10 — Chat Scanner Update for Instagram Handles

The `analyze-chat-intent` Edge Function already has regex for Instagram detection. Extend it to also return a `leakageDetected` flag so ChatScreen can show a retention nudge (not a block):

In `supabase/functions/analyze-chat-intent/index.ts`, find the regex section and add:

```typescript
// Existing regex — keep as is
const instagramRegex = /@[\w.]+/gi
const phoneRegex = /\b\d{10}\b|\b\d{3}[-.\s]\d{3}[-.\s]\d{4}\b/g

const hasInstagramHandle = instagramRegex.test(messagesText)
const hasPhone = phoneRegex.test(messagesText)

// Add leakage flag to the response
return new Response(JSON.stringify({
  shouldSuggestMeetup: confidence >= 75,
  confidence,
  triggerType,
  leakageDetected: hasInstagramHandle || hasPhone,  // NEW
}), { ... })
```

In `screens/ChatScreen.tsx`, when `leakageDetected` is true, show a soft banner (not a modal):

```tsx
{leakageDetected && (
  <View style={styles.retentionBanner}>
    <Ionicons name="shield-checkmark" size={16} color="#FF6B6B" />
    <Text style={styles.retentionText}>
      Keep chatting here — your messages are private and backed up on Rhome.
    </Text>
  </View>
)}
```

Do NOT block the message. Just nudge.

---

## Environment Variables Needed

Add to Replit environment:
```
EXPO_PUBLIC_INSTAGRAM_CLIENT_ID=your_meta_app_client_id
```

Add to Supabase Edge Functions → Secrets:
```
INSTAGRAM_CLIENT_ID=your_meta_app_client_id
INSTAGRAM_CLIENT_SECRET=your_meta_app_client_secret
INSTAGRAM_REDIRECT_URI=rhome://instagram-callback
```

---

## Files to Create / Modify Summary

| Action | File |
|--------|------|
| CREATE | `supabase/migrations/026_instagram_photos.sql` |
| CREATE | `supabase/functions/instagram-oauth/index.ts` |
| CREATE | `services/instagramService.ts` |
| CREATE | `components/InstagramBadge.tsx` |
| MODIFY | `components/RenterCard.tsx` — add InstagramBadge |
| MODIFY | `screens/MatchScreen.tsx` — add handle reveal |
| MODIFY | `screens/EditProfileScreen.tsx` — connect button + photo enforcement |
| MODIFY | `services/profileService.ts` — photo count guard |
| MODIFY | `supabase/functions/analyze-chat-intent/index.ts` — leakage flag |
| MODIFY | `screens/ChatScreen.tsx` — retention banner |

---

## Logic Summary

- `instagram_verified = true` → badge visible to ALL users on swipe card
- `instagram_handle` → visible ONLY after mutual match AND viewer has premium plan
- Free user + mutual match → blurred `@••••••••` with "Upgrade" button (peak intent paywall)
- Minimum 3 photos enforced at onboarding, profile save, and DB view
- Space photos optional but prompted — encouraged for hosts
- Chat leakage detection shows soft retention banner, never blocks message