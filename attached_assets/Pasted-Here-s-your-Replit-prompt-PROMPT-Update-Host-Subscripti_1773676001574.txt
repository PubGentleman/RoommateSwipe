Here's your Replit prompt:

---

**PROMPT: Update Host Subscription Screen to New 4-Tier Pricing — Renter Pricing Untouched**

Update only the host-side subscription/pricing screen to reflect the new 4-tier host pricing model. Do not modify any renter subscription screens, renter pricing logic, or renter plan constants. Preserve all mock data and isDev guards.

---

**1. Locate the host subscription screen**

Find the file that renders host plan options. It will be one of:
- `screens/host/HostSubscriptionScreen.tsx`
- `screens/host/HostPricingScreen.tsx`
- `screens/host/HostProfileScreen.tsx` (if pricing is embedded in the profile)
- Any screen under `screens/host/` that references host plan prices or subscription tiers

Do NOT touch any of the following renter files:
- `screens/renter/SubscriptionScreen.tsx`
- `screens/renter/PricingScreen.tsx`
- Any screen under `screens/renter/`
- `utils/renterPricing.ts` or equivalent
- Any constants file that contains `RENTER_PLANS` or renter-specific pricing

---

**2. Replace host plan data with the new 4-tier model**

Find wherever host plan pricing is currently defined (inline in the screen, or in a constants/utils file) and replace it entirely with:

```ts
const HOST_PLANS = [
  {
    id: 'free',
    label: 'Host Free',
    subtitle: 'Just getting started',
    price: 0,
    priceLabel: 'Free',
    listingsIncluded: 1,
    overagePerListing: 0,
    simultaneousBoosts: 0,
    freeBoostsPerMonth: 0,
    freeBoostDuration: null,
    badge: 'Free',
    badgeColor: '#888888',
    features: [
      '1 active listing',
      'Basic inquiry management',
      'Standard placement in search',
    ],
    lockedFeatures: [
      'Renter group browsing',
      'AI assistant',
      'Listing boosts',
      'Compatibility scores',
      'Verified host badge',
    ],
    ctaLabel: 'Get Started Free',
    isPopular: false,
  },
  {
    id: 'starter',
    label: 'Host Starter',
    subtitle: 'Homeowner with 1 room to fill',
    price: 19.99,
    priceLabel: '$19.99/mo',
    listingsIncluded: 1,
    overagePerListing: 0,
    simultaneousBoosts: 1,
    freeBoostsPerMonth: 1,
    freeBoostDuration: '24h',
    badge: 'Individual',
    badgeColor: '#60A5FA',
    features: [
      '1 active listing',
      'Renter group browsing',
      'AI assistant (host modes)',
      '1 free 24-hr boost/month',
      'Verified host badge',
      'Inquiry management',
      'Compatibility scores',
    ],
    lockedFeatures: [],
    ctaLabel: 'Get Started',
    isPopular: false,
  },
  {
    id: 'pro',
    label: 'Host Pro',
    subtitle: 'Own 2–5 units or rooms',
    price: 49.99,
    priceLabel: '$49.99/mo',
    listingsIncluded: 5,
    overagePerListing: 0,
    simultaneousBoosts: 3,
    freeBoostsPerMonth: 2,
    freeBoostDuration: '72h',
    badge: 'Most Popular',
    badgeColor: '#A78BFA',
    features: [
      'Up to 5 active listings',
      'Priority placement in search',
      'Renter group browsing',
      'AI assistant (host modes)',
      '2 free 72-hr boosts/month',
      'Up to 3 simultaneous boosts',
      'Advanced analytics',
      'Response rate tracking',
    ],
    lockedFeatures: [],
    ctaLabel: 'Get Started',
    isPopular: true,
  },
  {
    id: 'business',
    label: 'Host Business',
    subtitle: 'Landlord or property manager',
    price: 99,
    priceLabel: '$99/mo',
    listingsIncluded: 15,
    overagePerListing: 5,
    simultaneousBoosts: 10,
    freeBoostsPerMonth: 2,
    freeBoostDuration: '7d',
    badge: 'Professional',
    badgeColor: '#FBBF24',
    features: [
      'Up to 15 active listings',
      '+$5/mo per listing after 15',
      '2 free 7-day boosts/month',
      'Up to 10 simultaneous boosts',
      'Bulk boost across listings',
      'Full analytics suite',
      'Bulk messaging tools',
      'Agent verification badge (add-on)',
      'Priority support',
    ],
    lockedFeatures: [],
    ctaLabel: 'Get Started',
    isPopular: false,
  },
];
```

---

**3. Update the screen UI to render all 4 plans**

Render one card per plan in a vertical ScrollView. For each card:

**Free plan card:**
- Show plan name, subtitle, "$0/mo", "No credit card required"
- Show included features with ✓ checkmarks in gray
- Show locked features with ✕ marks, reduced opacity (0.5)
- Add an upgrade nudge at the bottom: small purple box with text "Upgrade to Starter to browse renter groups and fill your room faster"
- CTA: "Get Started Free" (outlined button)

**Starter, Pro, Business cards:**
- Show plan name, subtitle, price
- For Business only: show overage note "+$5/listing/mo after 15" in amber/yellow below the price
- Show all features with ✓ checkmarks
- Pro card: add "Most Popular" badge at the top
- CTA button: primary color for Pro, outlined for Starter, gold/amber for Business
- If this is the user's current plan: show "Current Plan" label, disable the CTA button

**Current plan indicator:**
- Read the user's current `hostSubscription.plan` from context/AsyncStorage
- Highlight the matching card with a colored border
- Change CTA to "Current Plan" (disabled) for the active tier

---

**4. Update any plan name/price references in the host profile screen**

Search for and update any hardcoded host plan prices or names in:
- `screens/host/HostProfileScreen.tsx` or `screens/host/ProfileScreen.tsx`
- Any component that displays "Your Plan: [name]" or "Current Plan: $X/mo" for host users
- The `HostPlanBadge` component if it has hardcoded plan labels

Replace old plan names (e.g. "Basic", "Standard", "Premium", "Pro $29/mo", "Business $59/mo", etc.) with the new names and prices from the HOST_PLANS array above.

---

**5. Update the host dashboard plan summary**

In `screens/host/HostDashboardScreen.tsx`, find where the current plan is displayed (usually a small summary card or banner). Update it to show:
- Free: "Free Plan · Upgrade to unlock all features"
- Starter: "Host Starter · $19.99/mo"
- Pro: "Host Pro · $49.99/mo"
- Business: "Host Business · $99/mo · [X] listings active"

---

**6. isDev mock data**

Ensure the following host mock users exist in StorageService or mock data:
```ts
// One host per tier for testing
mockHosts = [
  { id: 'host_free',     hostSubscription: { plan: 'free',     ... } },
  { id: 'host_starter',  hostSubscription: { plan: 'starter',  ... } },
  { id: 'host_pro',      hostSubscription: { plan: 'pro',      ... } },
  { id: 'host_business', hostSubscription: { plan: 'business', ... } },
]
```

---

**Guard rails:**
- Search the entire codebase for `RENTER_PLANS`, `renterSubscription`, `renter_plan`, and confirm zero changes were made to any of those
- Search for any shared pricing constants file — if host and renter plans are defined in the same file, split them into separate exports (`HOST_PLANS` and `RENTER_PLANS`) before editing, so changes to one never affect the other
- Do not change the Stripe product IDs or price IDs for existing renter plans
- All Supabase calls must have try/catch with StorageService fallback
- Do not remove or alter any existing renter subscription UI components

---

Paste that into Replit. It specifically scopes all changes to host-side files and includes the guard rail to verify renter pricing is untouched before finishing.