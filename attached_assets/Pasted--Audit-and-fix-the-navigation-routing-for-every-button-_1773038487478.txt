"Audit and fix the navigation routing for every button, tab, and link in the app. Go through each screen one by one and make sure every interactive element routes to the correct destination.
Check the following for each screen:
Bottom Navigation Bar — Make sure each tab navigates to the correct screen and that the active state (highlighted icon and label) updates to match whichever tab is currently open:

Explore tab → Explore/property search screen
Match tab → Roommate match/swipe screen
Groups tab → Groups discover screen
Chat tab → Messages screen
Profile tab → Profile screen

Welcome/Onboarding Screen — 'Get Started' and 'Next' button → next onboarding step. 'Skip' → main app (Match screen)
Sign In Screen — 'Sign In' button → Match screen on success, show inline error on failure. 'Forgot Password' → password reset screen. 'Sign Up' link → registration screen. Apple and Google buttons → OAuth login flow. Role selector (Renter / Host / Agent) → correct dashboard for that role on login.
Match Screen — Pass button (X) → dismiss card and load next. Like button (heart) → register like and load next. Message button → open chat with that user. Star button → save as favorite. Undo button → bring back previous card. City pill → city selector sheet. Filter icon → filter sheet.
Explore Screen — Property card tap → property detail screen. Save/heart button → save listing. 'All Properties' tab → full listing feed. 'Saved' tab → saved listings. Filter chips → apply filter and reload feed. City selector → city picker sheet. Map View button → map screen.
Groups Screen — Pass button → next group card. Like/join button → send join request. My Groups tab → user's joined groups. Create tab → create group screen.
Chat Screen — Conversation row tap → open that individual chat thread. New match bubble tap → open chat with that match. Search icon → search messages. Edit/compose icon → new message screen.
Profile Screen — Profile photo camera button → image picker. Edit Profile → profile edit screen. Notifications → notifications settings. Privacy & Safety → privacy screen. Account Settings → account screen. Upgrade button → subscription/paywall screen. Sign Out → sign out and return to sign in screen.
Host Dashboard — Make sure hosts who sign in with the Host role land on the Host Dashboard, not the renter Match screen. Check the following host-specific routes:

'My Listings' tab → host's active property listings
'Add New Listing' button → create listing form
Individual listing card tap → listing detail/edit screen
'Pause Listing' toggle → updates listing status in database and reflects on card
'Delete Listing' → confirmation modal, then removes listing and returns to My Listings
'Interested Renters' button on each listing → list of all renters who sent an interest card for that listing
Individual interest card tap → opens full renter profile with their compatibility score, budget, move-in date, and personal note
'Accept' button on interest card → unlocks chat with that renter and sends both users a match notification
'Pass' button on interest card → dismisses request and notifies renter
'My Matches' tab → all accepted renter connections
Matched renter row tap → opens chat thread with that renter
'Boost Listing' button → upgrade/paywall screen
'View Analytics' button → listing stats screen showing views, interest count, and match rate
Host profile settings → edit host profile, contact info, and verification status

After fixing all routing, do a final pass and confirm every back button and swipe-back gesture works correctly on both the renter and host sides. Report any routes that could not be connected due to missing screens."


Follow-up prompt to add after:

"Make sure that when a user switches roles between Renter, Host, and Agent on the sign in screen, they are always directed to the correct dashboard for that role and never see screens intended for a different role."