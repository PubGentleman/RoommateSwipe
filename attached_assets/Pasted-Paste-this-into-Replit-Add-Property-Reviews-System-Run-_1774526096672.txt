Paste this into Replit:

---

**Add Property Reviews System**

Run this SQL first in Supabase SQL Editor:

```sql
CREATE TABLE IF NOT EXISTS public.property_reviews (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  listing_id UUID NOT NULL REFERENCES public.properties(id) ON DELETE CASCADE,
  reviewer_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  rating INTEGER NOT NULL CHECK (rating >= 1 AND rating <= 5),
  review_text TEXT,
  tags TEXT[] DEFAULT '{}',
  host_reply TEXT,
  host_replied_at TIMESTAMPTZ,
  helpful_count INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(listing_id, reviewer_id)
);

ALTER TABLE public.property_reviews ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can read reviews"
  ON public.property_reviews FOR SELECT USING (true);

CREATE POLICY "Reviewer can insert own review"
  ON public.property_reviews FOR INSERT
  WITH CHECK (reviewer_id = auth.uid());

CREATE POLICY "Reviewer can update own review within 24 hours"
  ON public.property_reviews FOR UPDATE
  USING (reviewer_id = auth.uid() AND created_at > NOW() - INTERVAL '24 hours');

CREATE POLICY "Host can update reply on their listing"
  ON public.property_reviews FOR UPDATE
  USING (
    listing_id IN (
      SELECT id FROM public.properties WHERE host_id = auth.uid()
    )
  );

ALTER TABLE public.properties
  ADD COLUMN IF NOT EXISTS average_rating NUMERIC(2,1) DEFAULT NULL,
  ADD COLUMN IF NOT EXISTS review_count INTEGER DEFAULT 0;
```

---

**Create `screens/shared/PropertyReviewsScreen.tsx`:**

Full reviews screen showing:
- Large average rating number (e.g. 4.6) with 5 stars
- Star breakdown bar chart (5★ → 1★ with counts and fill bars)
- "Write Review" button — only shown if user is eligible (has an existing conversation with the listing's host)
- List of review cards each showing:
  - Reviewer avatar (first letter of name), name, stay duration, date
  - Star rating
  - Review text
  - Tags as small chips (Clean, Responsive host, Great location, Noisy, Maintenance issues, Good value, Pet friendly, Quiet building)
  - 👍 Helpful button that increments `helpful_count`
  - ⚑ Report button
  - Host reply section if `host_reply` is set — shown with a red left border and "Host Reply" label above the reply text

---

**Create `components/WriteReviewSheet.tsx`:**

Bottom sheet modal with:
- 5 tappable stars (tap to select rating, selected stars turn red)
- Multiline text input placeholder: "Share your experience living here..." (optional, max 500 chars)
- Tag multi-select chips: Clean, Responsive host, Great location, Noisy, Maintenance issues, Good value, Pet friendly, Quiet building
- Submit button — disabled until at least a star rating is selected
- On submit: insert row into `property_reviews`, then recalculate and update `average_rating` and `review_count` on the listing

---

**Update listing detail screen:**
- Add a reviews section below the listing details showing the rating summary and first 3 reviews
- "See all X reviews" link navigates to `PropertyReviewsScreen`
- If no reviews: show "No reviews yet — be the first"

---

**Update listing cards in ExploreScreen and anywhere listings appear as cards:**

IMPORTANT — do NOT change the existing card layout, styling, design, or structure in any way. Only add a small star rating badge overlaid on the bottom-left corner of the existing listing image, using absolute positioning so it sits on top without affecting layout:

```typescript
{listing.review_count > 0 && (
  <View style={styles.ratingBadge}>
    <Text style={styles.ratingText}>⭐ {listing.average_rating?.toFixed(1)} ({listing.review_count})</Text>
  </View>
)}

ratingBadge: {
  position: 'absolute',
  bottom: 10,
  left: 10,
  backgroundColor: 'rgba(0,0,0,0.65)',
  borderRadius: 20,
  paddingHorizontal: 10,
  paddingVertical: 4,
},
ratingText: {
  color: '#fff',
  fontSize: 12,
  fontWeight: '600',
}
```

If `review_count` is 0, show nothing on the card — no badge, no "No reviews" text.

---

**Host reply feature:**
In the host's listing management area, show each review with a reply input. Host can add one reply per review. Saves to `host_reply` and `host_replied_at`.

No additional migrations needed beyond the SQL above.

---

Let me know when done.